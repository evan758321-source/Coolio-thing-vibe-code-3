#pragma once

namespace FriendLeaderboard {

    // Registers the BSML mod settings panel (login, toggles, etc.)
    class ModSettings {
    public:
        static void Register();
    };

} // namespace FriendLeaderboard
